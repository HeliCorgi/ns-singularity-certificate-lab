# Lean 4 最終検証ポリシー

## 目的

本プロジェクト `ns-singularity-certificate-lab` では、数値計算、AI探索、区間演算、解析によって得た結果を、最終的に Lean 4 で機械検証可能な定理へ接続する。

Lean 4 を「候補を発見する主な道具」ではなく、「最終的な論理接続と証明書を検査する最終ゲート」として使用する。

## 重要な制限

Lean 4 が保証するのは、Lean 4 に入力された定義・仮定・公理から、指定された命題が導出できることである。

Lean 4 は自動的には次を保証しない。

- 形式化した命題が Clay Mathematics Institute の問題文と一致していること
- 円柱座標や再スケーリングの定義が物理的な3次元方程式と一致していること
- Python、JAX、Julia、CUDAなどの数値計算が正しいこと
- 浮動小数点結果が厳密な実数の主張を意味すること
- 外部ファイルや証明書の生成過程が正しいこと
- 採用した公理が数学界で受け入れられる範囲にあること

したがって、最終的な信頼性は次の三つをそろえて成立させる。

1. **定理の忠実性**  
   Lean上の最終命題がClayの公式問題と正確に対応している。

2. **証明書の忠実性**  
   数値候補が有理数・二進有理数・区間などの厳密な有限データへ変換され、Lean内の検証器が必要な不等式を確認する。

3. **カーネル検証**  
   最終証明がLean 4の小さなカーネルで受理される。

## 使用する環境

- Lean 4
- mathlib4
- Lake
- リポジトリに固定された `lean-toolchain`
- リポジトリに固定された依存関係
- CI上での再現可能な `lake build`

Lean 3は新規採用しない。

## リポジトリ構成

将来、次の構成を追加する。

```text
formal/
├── README.md
├── lakefile.toml
├── lean-toolchain
├── Main.lean
└── NSSingularity/
    ├── ClayStatement.lean
    ├── BasicDefinitions.lean
    ├── VectorCalculus.lean
    ├── NavierStokes.lean
    ├── Axisymmetric.lean
    ├── Rescaling.lean
    ├── FunctionSpaces.lean
    ├── LocalExistenceBridge.lean
    ├── CertificateFormat.lean
    ├── IntervalChecker.lean
    ├── OperatorBounds.lean
    ├── OrbitExistence.lean
    ├── FiniteTime.lean
    ├── BlowupCriterion.lean
    └── FinalTheorem.lean
```

## 段階的な形式化

### 段階0：Clay命題を固定する

最初に、Clayの公式問題に対応する命題をLean上に記述する。

最低限、次を区別する。

- `ℝ³` の外力なし大域正則性
- 3次元周期領域の外力なし大域正則性
- `ℝ³` の反例
- 3次元周期領域の反例
- 外力あり反例
- 外力なし反例という、公式要件より強い結果

最終命題の自然言語対応表を `formal/README.md` に置く。

この段階では証明しなくてよい。定義と命題の対応を独立した数学者が査読できる状態にする。

### 段階1：有限次元の恒等式

先に小さく明確なものを形式化する。

- 円柱座標とCartesian座標の成分変換
- 速度回復式から発散ゼロが従う恒等式
- 軸対称変数間の代数的関係
- 再スケーリングの指数計算
- 有限物理時間条件
- 候補証明書の有限次元不等式

### 段階2：解析的な橋

次を段階的に形式化する。

- 微分可能性
- Sobolev空間・必要な関数空間
- 発散ゼロ
- Navier–Stokes方程式を満たすこと
- 座標変換の同値性
- 局所解との接続
- 再スケーリング解から物理解への再構成
- 有限時間でのノルム発散

mathlibに不足する理論は、プロジェクト固有の定理として追加する。ただし、未証明の主張を公理として追加しない。

### 段階3：数値証明書

発見コードは、最終候補を次のような厳密データとして出力する。

- 整数
- 有理数
- 二進有理数
- 上端・下端が厳密数である区間
- 有限個のスペクトル係数
- スペクトル尾部の解析的上界
- 残差の区間上界
- 逆作用素ノルムの上界
- 非線形Lipschitz定数
- Newton–Kantorovichまたは縮小写像の条件

Lean側は、外部計算を信用せず、証明書に含まれる有限個の主張を独立に検査する。

### 段階4：最終定理

最終定理は、少なくとも次を接続する。

1. 明示的な滑らかで発散ゼロの初期値
2. 必要なら有限エネルギー・減衰条件
3. 再スケーリング軌道の厳密な存在
4. 元の3次元Navier–Stokes方程式との同値性
5. 有限の物理時刻
6. `t < T` での滑らかさ
7. `t ↑ T` での適切なノルムの発散
8. Clayの反例命題

## 禁止事項

最終証明経路では、次を禁止する。

- `sorry`
- `admit`
- プロジェクト固有の未証明 `axiom`
- 証明対象の核心を仮定する定義や定理
- 未監査の `unsafe` コードを正しさの根拠にすること
- 浮動小数点値を、そのまま厳密な実数証明として使うこと
- Python側の `assert` を証明として扱うこと
- ニューラルネットの出力を証明として扱うこと
- 外部ソルバーが「成功」と返したことだけを証明として扱うこと
- 同じ実装で候補生成と証明書検査を完結させること
- `lake build` が通るだけでClay問題を正しく形式化したとみなすこと

## 必須の監査

CIで次を確認する。

```bash
lake build
```

さらに、最終証明経路に対して次を監査する。

- `sorry` と `admit` が存在しない
- プロジェクト固有の未証明公理が存在しない
- 最終定理が依存する公理を `#print axioms` で記録する
- 使用するLeanとmathlibのバージョンが固定されている
- 証明書のハッシュが固定されている
- 別環境で同じ証明が再構築できる
- 可能なら独立したLean証明チェッカーでも確認する
- Clay命題との対応表を数学者が独立査読する

## 完了条件

「Leanで検証済み」と表現してよいのは、次をすべて満たす場合だけである。

- 最終命題がClayの公式問題に忠実である
- 必要な数学的定義が形式化されている
- 数値部分が厳密な証明書へ変換されている
- 証明書をLean側が独立検査している
- 最終証明に `sorry`、`admit`、未証明の核心的公理がない
- 固定された環境で `lake build` が成功する
- 最終定理の依存公理が公開されている
- 別環境で再検証できる
- 人間向けの証明概要とLean定義の対応表がある

## AIエージェントへの指示

数値探索を止めてLean全面形式化へ移行してはならない。

現在の優先順位は次である。

1. 数学的モデルの監査
2. 正しい数値ソルバー
3. Hou候補の再現
4. 境界依存試験
5. `ℝ³` 候補探索
6. 動的再スケーリング軌道の発見
7. 厳密な区間証明書の設計
8. Lean 4による証明書検査と論理接続
9. 最終定理

ただし、各段階で将来Lean化する命題と必要な仮定を `docs/formalization_map.md` に記録する。

候補が得られるまでLeanを無視するのではなく、小さな恒等式と最終命題の仕様は早い段階で形式化する。一方、未知のPDE機構全体を候補発見前に形式化しない。
